# Local development environment variables
#
# Copy this file to a local `.env` (do not commit `.env`).
# For this monorepo, Expo runs from `apps/sudoku`, so place it at:
#   - apps/sudoku/.env
# (Optionally also keep a repo-root `.env` if other tooling expects it.)
#
# When using local Supabase (CLI + Docker), start it and then run `supabase status`
# to copy the local anon key into this file.
#
# Copy commands:
# - macOS/Linux:
#     cp docs/env.example apps/sudoku/.env
# - Windows (PowerShell):
#     Copy-Item docs/env.example apps/sudoku/.env
#
# IMPORTANT:
# - Only variables prefixed with EXPO_PUBLIC_ are bundled into the Expo app/web build.
# - Never commit real secrets. Replace the placeholders locally / in provider UIs.

# --- Supabase (client-safe) ---
# Local defaults (Supabase CLI):
EXPO_PUBLIC_SUPABASE_URL=http://localhost:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=REPLACE_ME_WITH_LOCAL_ANON_KEY_FROM_SUPABASE_STATUS
EXPO_PUBLIC_SUPABASE_FUNCTIONS_URL=http://localhost:54321/functions/v1
#
# Hosted Supabase (optional):
# EXPO_PUBLIC_SUPABASE_URL=https://REPLACE_ME.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=REPLACE_ME_ANON_KEY
# EXPO_PUBLIC_SUPABASE_FUNCTIONS_URL=https://REPLACE_ME.supabase.co/functions/v1

# --- OAuth (optional / TODO) ---
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=REPLACE_ME_GOOGLE_WEB_CLIENT_ID
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=REPLACE_ME_GOOGLE_IOS_CLIENT_ID
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=REPLACE_ME_GOOGLE_ANDROID_CLIENT_ID

# --- Sudoku puzzles (static hosting / CDN) ---
# Daily puzzles + manifest (see PRD §6.1). Example:
# - manifest: <base>/manifest.json
# - payloads: <base>/daily/YYYY-MM-DD.json (URLs are provided by the manifest)
# Recommended (Netlify): serve these under the same site but outside the app router, e.g.
# https://sudoku.cynnix.com/puzzles/daily
EXPO_PUBLIC_SUDOKU_DAILY_BASE_URL=https://REPLACE_ME.example.com/puzzles/daily
#
# Free Play packs + manifest (see PRD §6.2). Example:
# - manifest: <base>/manifest.json
# - pack payloads: <base>/<difficulty>/<version>.json (URLs are provided by the manifest)
EXPO_PUBLIC_SUDOKU_FREEPLAY_BASE_URL=https://REPLACE_ME.example.com/sudoku/freeplay


